name: flip_drawer_3d
description: A new Flutter package for 3d transition effect while flipping the drawer.
version: 0.0.3
homepage: https://github.com/iskeletorr/flip_drawer_3d

environment:
  sdk: '>=2.18.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true